export interface ThemeConfig {
  background: {
    base: string;
    accent: string;
    glow: string;
  };
  cake: {
    frosting: string;
    highlight: string;
    shadow: string;
  };
  text: {
    primary: string;
    secondary: string;
  };
}
