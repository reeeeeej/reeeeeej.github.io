import type { SceneConfig } from '../types/scene';

export const sceneConfig: SceneConfig = {
  title: 'Happy Birthday',
  subtitle: '',
  hintText: '轻触蛋糕，打开相册',
  maxInteractiveCards: 12,
  totalCardCount: 30,
  enableParticles: true,
  enableCakeGlow: true,
};
